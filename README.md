# 中醫嘉年華｜體質探索站 V8

上傳 GitHub Pages：
1. 建立新 repository，例如 `tcm-carnival-v8`。
2. 將本資料夾中的 `index.html` 與整個 `assets` 資料夾一起上傳。
3. 到 Settings → Pages，選擇 Branch: `main`、Folder: `/root`。
4. 等待部署完成後，開啟 GitHub Pages 網址。

注意：`assets` 資料夾名稱不可改，圖片檔名也請保持不變。
